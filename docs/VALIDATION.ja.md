# 再設計版の検証

[詳細（English）](VALIDATION.md) · [設計](design/FAULT_SYSTEM.ja.md)

公開release `7dd83a7`を基準にした未公開実装を、2026-09-08に検証しました。

- JVMは4種類のfixture × 4 variant、計16テストが成功。
- F-Droid debug／unsigned release、Play debug、端末テストAPKのビルド成功。lintはfatal／errorなし。警告は残っています。
- 名前付き操作値、個体の固定・RESEED分離、連続drift、一時事故、snapshot再読、経路順、RAW値域・Bayer位相、表示画像の予約と有界保持を検証。
- エミュレーターでは13 shaderと全操作項目、編集の適用／キャンセル、言語切替、カメラ復旧、LIVE入力の後始末、短い動画保存を確認。
- 表示画像を予約後にカメラと設定を進めても、保存JPEGが予約画像を同じ品質で圧縮したものと画素一致。camera timestampとFAULT情報も一致。

JPEGは実ライブ信号の解像度です。RAWは別露光・別表現で、RGBプレビューと同一ではありません。STREAMは復号画像内のサンプル再利用モデルで、実packet破壊や過去フレームdatamoshはありません。

この再設計について、物理端末の温度・センサー応答・RAW保存、長時間／高解像度の持続性能は未検証です。旧releaseの実機結果は[過去の記録](audit/VALIDATION_1_0_0.md)として区別します。

## 2026-09-08 プレビュー・編集UIの実機確認

端末25060RK16C / Android 16で、PIXEL DAMAGEが平坦な入力の約62%を変えてしまう不具合を再現しました。GPUの乱数計算を修正後、3つの個体差・2解像度で点と列が約6.9〜8.9%に収まりました。RAWの整数処理は変更していません。

編集パネルとプレビューが重ならないこと、スライダーのライブ反映、複数選択、ランダムチェーン、適用・キャンセル、古いダイアログの終了が新しい編集を壊さないことを確認しました。最大ライブ解像度3072×4096でチェーン付きJPEGを保存し、13種類のエフェクトと表示フレームの保存一致も再確認しました。単体テストは合計20件で失敗0件です。長時間録画や他のGPU・端末は別途確認が必要です。

## チェーンUI整理 — 2026-09-08

接続端末（Android 16 / API 36）の開発版で、編集パネルとプレビューの非重複、スライダー操作、適用・取消、ランダム生成、古いダイアログの終了処理、RAWでの全13種類表示（CRT / DISPLAYを含む）、JPEG切り替え時のドラフト保持、JPEG–RAW間のチェーン保持を確認しました。状態・ライフサイクルと表示フレームのJPEG保存一致のテストも通過。RAW一覧とCRT調整の実機画面を目視確認しました。長い操作欄はスクロールできます。単体テスト、lint、両配布先のデバッグビルド、計装テストビルド、301文字列の翻訳チェックが通過しました。提出済み成果物と署名鍵は変更していません。

## JPG／RAWと設定画面の統一 — 2026-09-08

写真形式をJPGとセンサー加工RAWの2択にし、以前のRAW原本設定はRAWへ移行します。実機で専用形式パネルの選択・取消・設定移行・トグル操作、形式間のチェーン保持を確認。4096×3072のDNG保存も成功しました。形式・LIVE・撮影設定の画面を目視確認し、アプリ内の標準アラート・ポップアップメニュー・スイッチを専用パネルと統一デザインへ置換しました。両デバッグビルド、単体テスト、lint、301文字列の翻訳チェックが通過しました。OS権限画面や外部アプリ画面はAndroid側のUIです。

## 共通カメラアルバム・LIVE演出・ADVANCED MODE — 2026-09-08

接続中のAndroid 16／API 36端末で、JPG（1080×1920）、加工DNG（4096×3072）、MP4（3072×4096、短時間・無音）を撮影し、MediaStore上の保存先がすべて `DCIM/5igna1/` であることを確認しました。既存ファイルは移動せず、実験的RAW ZIPはDownloadのままです。

LIVEは下書きの独立性・適用／取消・設定保存、カメラ時刻を進めたままのHOLD、HIT、時間リセット、プレビューを覆わない配置を実機確認しました。ADVANCEDは厳密な数値入力・範囲外拒否・下書き／保存、符号付き64ビットEVENT SEEDの入力と永続化、pixelDensityのGPU反映、全13FAULTのSIGNAL／PROFILE最小・最大値の描画が成功しました。既存エディター、状態・ライフサイクル、表示フレームJPG固定の確認も成功しました。ADVANCEDとLIVEの画面を目視確認しました。

単体検証は全内部パラメータの網羅性・範囲・AUTO／FIX・スナップショット不変性・RAW反映・seed保存と再現性・時間のループ／逆行／ステップ・演出包絡・プレビュー分離を確認します。両debug版、単体検証、lint、338翻訳文字列、リポジトリ検査、差分空白検査が成功しました。実機1台と短時間撮影の範囲で、長時間録画や他端末の性能を保証する検証ではありません。提出済み成果物・署名・公開タグは変更していません。

設定画面の反復検証で、この端末がTextureView更新通知を返さず、実際の表示時刻より保持履歴の確認時刻が古いままになる条件を確認しました。前面動作中のみ100 msごとにUIが取り込んだSurfaceTextureの時刻を確認して復帰します。未表示のGL送信画像を表示済み扱いにはしません。ADVANCED実機検証では更新通知を意図的に省略し、撮影スナップショット・状態／ライフサイクルも再確認します。

## LIVEの時間変化としての整理・ADVANCED一括設定 — 2026-09-08

LIVEは時間の進み方を先に置き、変化の形を挙動が伝わる名前へ変更しました。周期・更新間隔は秒単位で、以前のテンポ／拍の設定は同じ時間長へ移行します。一時停止／再開・故障を発生・時間リセットを等幅・等間隔にしました。ADVANCED MODEは設定の一括切り替えに集約し、FAULTごとの切り替えを削除しました。実機で一括設定の適用／取消／保存、個別スイッチの撤去、秒単位LIVE設定の永続化・時間挙動、エディター配置が成功しました。設定・LIVEパネル・メイン画面の時間操作列を目視確認しました。両debug版、単体検証、lint、343翻訳文字列・リポジトリ検査が成功しました。

## 負荷の自動調整と端末推奨設定 — 2026-09-08

接続中のAndroid 16／API 36端末では、推奨JPG・動画は1080×1920でした。実機カメラと同じPixel Damage設定で、高温情報を模擬すると2秒間のプレビュー加工回数が47〜48回から12回へ減りました。加工回数の測定であり、温度や消費電力の削減率ではありません。LIVEがOFFでも動作し、解像度とFAULT状態を保持しました。限界温度の模擬入力では描画・撮像を休止し、冷却状態の継続を模擬するとプレビューが復帰しました。推奨1080×1920のMP4録画も限界温度への遷移で正常に終了・保存され、DCIM/5igna1へ入り、冷却後に録画が勝手に再開しないことを確認しました。カメラ一覧・手動最大・低解像度フォールバック・前後カメラ復帰・表示フレームJPGの画素／状態固定・ライフサイクルの検証も成功しました。

推奨設定ボタンの下書き取消、従来の最大設定の移行、明示サイズ／最大の保持、推奨画素数上限が成功しました。設定・更新頻度低下・冷却休止の画面を確認しました。単体検証は更新間隔、素早い負荷低下と緩やかな回復、温度／バッテリー／余裕予測、休止と冷却待ち、描画コスト制限を対象にしています。両debug版の検証とlint、351翻訳文字列、リポジトリ・空白検査が成功しました。Pixel 6 Proは未接続のため、同機種の温度・電力・長時間録画は未検証です。OSの温度設定を変更したり、端末を故意に加熱したりせず、アプリ内の温度入力を模擬しています。提出済み成果物と署名は変更していません。

## EXPERT MODE / settings — 2026-09-08

EXPERTの下書き取消・適用・永続化、プレビュー描画（2秒間に59回）、限界温度と高い描画コストを模擬しても録画を継続すること、手動終了・保存、OFFに戻した際の通常の温度保護への復帰を実機確認しました。ADVANCEDと通常の負荷制御の回帰確認も成功しました。設定の見出し・余白・説明の配置を目視確認しました。アプリ内入力の模擬による確認であり、Androidの温度制御は変更しておらず、長時間の端末性能を保証するものではありません。

パネル・操作ボタン・入力欄・プレビュー枠の角丸を12 dp、小さなバッジを4 dpに共通化しました。書体を共通のサンセリフにし、見出し18 sp・設定ラベル13 sp・説明文12 spを基準に、行間と日本語見出しの字間を整理しました。FAULT一覧は文字を拡大して2列へ変更しました。実機のEXPERT（最終版では2秒間58回描画）、ADVANCED、全13種類のRAW一覧／JPG往復の確認が成功しました。設定とFAULT画面を目視確認し、単体検証・lint・両debugビルド・364翻訳文字列の検査が成功しました。

LIVEの適用／取消・時間操作・プレビューを覆わない配置も成功し、LIVEとADVANCEDの文字組みを同じ端末で目視確認しました。

## 撮影画面と写真・動画共通プレビュー — 2026-09-08

形式メニューをボタン直下へ移し、ライト・GPS・音声を上部のアイコンにしました。音声は写真モードでも表示し、大きなロゴを撮影画面から外しました。FAULT STATEはプレビューを覆わないメーター表示です。メニュー位置・形式選択／取消、上部配置・音声切り替え、メーター行、編集の適用／取消、カメラ切断と撮像停止を模擬した復帰確認が成功しました。

外部の動画閲覧では、閉じる操作後も再生アプリが撮影アプリのタスクに残り、撮影アプリを起動し直しても外部ビューアーへ渡される状態を確認しました。通常の写真・動画閲覧をアプリ内へ統一し、明示的な外部閲覧は別タスクに分離しました。接続中のAndroid 16端末でJPEGと1080×1920／30 fpsのMP4を保存し、実際の左右スワイプで相互に切り替え、動画の再生・一時停止・シークを確認しました。動画の下スワイプ終了を6回繰り返し、その都度プレーヤーとSurfaceを解放し、新たに表示されたカメラフレームへの復帰を確認しました。読み込み中の即時終了、読み取れない動画URI、バックグラウンド移行時の解放、その後の録画・保存も成功しました。最初の確認時は写真と動画を含む43件を一覧化しました。写真・動画・復帰後の画面を目視確認しました。暗いテスト素材は実際の撮影対象によるものです。

単体検証・lint・両debugビルド・380翻訳文字列・リポジトリ検査が成功しました。実機での短時間確認であり、すべてのデコーダーや長時間再生を保証する検証ではありません。提出済み成果物・署名・公開タグは変更していません。

## 1.1.0への統合確認 — 2026-09-08

次版を1.1.0 / versionCode 9に更新しました。lint・test、F-Droid debug／署名なしrelease、Play debug、実機テストAPKのビルド、FOSS依存検査に成功しました。版情報、380項目の翻訳、管理対象ファイル、依存一覧、差分形式を確認し、APKのID・版情報、文書内リンク267件、3言語のプライバシーHTMLとアプリ内本文の一致も確認しました。実装動作は上記の実機検証済みで、今回の統合作業は文書と版情報の更新です。公開済みv1.0.0と提出済みcode 8の成果物を維持し、新しい署名・ストア提出・タグ・リリース公開は行っていません。ストア画像は1.0.0のため、次回提出前に更新が必要です。


## 開発版チュートリアル — 2026-09-09

`codex/onboarding-tutorial`に追加。提出済みPlay 1.1.0 / code 9には含まれず、mainへの統合・ストア提出は行っていません。今回は実機を接続せず、Pixel 9 / API 37エミュレーターで検証しました。

- 初回表示、前後移動、スキップ・完了後の再表示抑制、システムの戻る、3ページ目での画面再生成と位置復元、設定からの再表示、設定の未確定変更の保持、カメラへの復帰に成功。
- カメラ未許可でも説明を読め、閉じた後に権限確認が表示されることを確認。
- 日本語・英語・簡体字中国語の全5ページを標準／1.5倍の文字サイズで目視確認。本文は操作ボタンとは独立してスクロール可能です。
- lint、テスト、F-Droid debug／unsigned release、Play debug、端末テストAPKのビルドに成功。395文字列の翻訳・リポジトリ・リリース情報・差分チェックも成功。
- 実行はdebugアプリのみ。既存の署名済み提出物・main・公開タグは維持。次回提出時には新しいバージョン番号／コードが必要です。

## Kotlin全面移行 — 2026-09-09

チュートリアル開発版を基点とする`codex/kotlin-cleanup`で、アプリ本体と全テストをKotlin 2.2.20へ移行しました。本体は44ファイルで、画面構築、完成した撮影データの保存、不変コレクションのコピーを分離しています。シェーダーはGLSL、ビルド・公開補助は従来の適切な言語を維持します。

- クリーン状態からlint、両配布版のテスト、F-Droid debug／unsigned release、Play debug、端末テストAPK、FOSS依存チェック、依存一覧の生成に成功。全4構成×9件、計36件のJVMテストに成功しました。lintエラーは0件で、既存のリソース・書式等の警告は残ります。
- 移行前Javaコミット`c0ce2eb`で固定したSHA-256と照合。全エフェクトID・4段階のLEVEL・120フレーム・3サイズのRAWデータで、評価結果とRAWバイト列が一致しました。Kotlin側の出力で期待値を作り直していません。
- Pixel 9 / API 37で`state`、`effects`、`pixel-preview`、`advanced`、`product-ui`、`capture-contract`、`mixed-preview`、`tutorial`に成功。適用／取消、13 FAULTと全操作値、画素配置、詳細操作、カメラ復旧、シャッター時の表示画像と保存JPEGの画素一致・メタデータ、写真／動画の切替・再生・解放、チュートリアルと言語を確認しました。
- 撮影検証で見つけた、元JPEGメタデータのバイト列に対する不要な必須チェックを修正しました。表示画像の保存では元JPEGがないことを許容します。チュートリアルのシステム戻る検査は入力フォーカスと閉じる完了を待つようにし、画面表示直後のタイミング競合を解消しました。
- 依存一覧と同梱ライセンスを更新し、KotlinのThreeTen由来部分のBSD表記を追加しました。配布版ごとのruntime依存グラフは同一、翻訳確認は395文字列です。
- 実機は未接続。RAWバイト列の検証に成功していますが、ハードウェアRAW／DNG／RAW動画は今後の実機確認が必要です。エミュレーターはdebugアプリのみを使用し、main・提出済み署名物・既存の鍵・公開タグを保持しています。ストア提出は行っていません。

## Kotlin版の実機確認 — 2026-09-09

所有者がUSB接続したAndroid 16端末（model `25060RK16C`、device `dali`）でインストールとデバッグを実施。`43ee376`のKotlin開発版を「5igna1 DEV」としてインストールし、公開版は置き換えていません。

- `capture-contract`、`raw-original`、`raw`、`raw-video`、`video`、`state`、`advanced`、`product-ui`の8項目に成功。前後カメラのRAW対応を取得し、撮影は選択中の背面カメラで検証しました。
- 原本／加工DNGは4096×3072で保存し、PC側のLibRawで読み込み・展開・現像に成功。加工はROW ERROR、LEVEL 70%です。
- 短いRAW動画では原本DNGを2フレーム記録し、寸法・時刻・manifest・ZIPの終了処理が一致しました。長時間録画や最大速度の検証ではありません。
- 無音MP4は1080×1920、3.029秒、30.05fps。全フレームのデコード、時刻の単調増加、動画開始時刻を確認しました。既存の負荷制御で画面プレビューのfpsは低下しましたが、保存動画は約30fpsです。
- JPEGはシャッター時に確保した表示画像と画素・メタデータが一致。編集の適用／取消、詳細操作、カメラ中断と停止したプレビューからの復帰も成功し、この実行では新たなアプリ不具合は見つかりませんでした。撮影物は端末の共有5igna1フォルダーに残し、撮影設定は復元しています。
